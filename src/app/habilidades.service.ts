import { HttpClient } from '@angular/common/http';
import { Injectable } from '@angular/core';
import { Habilidade } from './habilidades/habilidade.interface';

@Injectable({
  providedIn: 'root'
})
export class HabilidadesService {
  
 /* habilidade: Array<Habilidade>
  habilidades=[];*/
  constructor(
    

   // private http: HttpClient
  ) { }

 /* getDados(nome:string,descricao:string){
    this.habilidades.push(nome);
    this.habilidades.push(descricao);
}*/
}
